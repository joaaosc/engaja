# Database

Diretorio reservado para a persistencia do sistema.

Subareas previstas:

- `schema/`: definicao estrutural
- `migrations/`: evolucao versionada
- `seeds/`: carga inicial e dados de apoio
- `data/`: armazenamento local do arquivo `reports.json` gerado pela API
